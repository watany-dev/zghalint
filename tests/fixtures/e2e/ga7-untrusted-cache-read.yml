# zghalint:forbid SEC024 SEC016
name: untrusted
on: pull_request_target
permissions:
  contents: read
cache-mode: read
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo restore-only default kept
