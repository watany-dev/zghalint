# dependabot.yml は actionlint が読まないため actionlint だけ skip される。
# bench:expect dependabot-cooldown@5
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
