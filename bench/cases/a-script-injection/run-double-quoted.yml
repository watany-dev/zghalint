# bench:expect template-injection@13
name: double quoted
on:
  issues:
    types: [opened]
permissions: {}
jobs:
  triage:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Echo the title
        run: "echo ${{ github.event.issue.title }}"
