# bench:expect template-injection@15
name: block folded
on:
  issues:
    types: [opened]
permissions: {}
jobs:
  triage:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Echo the title
        run: >
          echo "start" &&
          echo "${{ github.event.issue.body }}"
