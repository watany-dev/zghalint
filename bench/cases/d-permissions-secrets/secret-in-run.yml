# bench:expect secrets-outside-env@12
name: secret in run
on: push
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Publish with the token inline
        run: npm publish --token ${{ secrets.NPM_TOKEN }}
