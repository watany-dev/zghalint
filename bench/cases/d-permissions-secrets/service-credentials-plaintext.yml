# `services:` 側の credentials も同じ扱いになるべき。
# bench:expect hardcoded-container-credentials@16 zizmor=hardcoded-container-credentials actionlint=credentials
name: service credentials
on: push
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      registry:
        image: ghcr.io/example/registry@sha256:6b3f4d4e5ae03e0e8f8e2f0b1c5e2a2e0b4c8f5d5a3f2e1d0c9b8a7f6e5d4c3b
        credentials:
          username: builder
          password: hunter2hunter2
    steps:
      - name: Test
        run: make test
