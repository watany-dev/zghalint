# bench:expect unknown-event@5
# bench:fix-allow --fix-unsafe zizmor=dangerous-triggers 候補が特権トリガのときは --fix-unsafe でのみ置換する (G22 / #346)
name: unknown event
on:
  pull_request_targt:
    types: [opened]
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Build
        run: make build
