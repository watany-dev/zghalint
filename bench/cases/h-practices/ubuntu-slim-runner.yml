# bench:forbid unknown-runner-label zghalint=RUNNER002 zizmor=- actionlint=-
name: lint
on: push
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  lint:
    runs-on: ubuntu-slim
    timeout-minutes: 10
    steps:
      - name: Lint
        run: echo ok
